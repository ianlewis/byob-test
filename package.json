{
  "name": "@ianlewis/byob-test",
  "version": "0.0.1",
  "description": "Predicate type declarations and helpers",
  "keywords": [],
  "author": "Ian Lewis",
  "main": "dist/index.js",
  "files": [
    "/dist"
  ],
  "license": "Apache-2.0",
  "scripts": {
    "ci": "npm ci",
    "build": "tsc",
    "format": "prettier --write '**/*.ts'",
    "format-check": "prettier --check '**/*.ts'",
    "package": "ncc build --source-map",
    "lint": "eslint src/**/*.ts",
    "all": "npm ci && npm run format && npm run lint && npm run build && npm run package",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^4.8.4"
  }
}
